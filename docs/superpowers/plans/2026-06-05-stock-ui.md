# SocialDex 股票风格 UI 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 SocialDex 前端重写为 Vue 3 SPA，采用股票软件风格的交互界面。

**Architecture:** 后端 Python 生成丰富的 data.json（tag 汇总 + 单账号曲线 + 涨跌幅），前端 Vue 3 SPA 通过 fetch 加载数据，Lightweight Charts 渲染图表，DaisyUI 提供组件样式。build 产物输出到 frontend/ 目录。

**Tech Stack:** Bun, Vue 3 + TypeScript, Vite, DaisyUI + Tailwind CSS, Lightweight Charts, Python (generate.py)

---

## Task 1: 初始化 Vue 3 项目

**Files:**
- Create: `web/package.json`
- Create: `web/index.html`
- Create: `web/vite.config.ts`
- Create: `web/tsconfig.json`
- Create: `web/tsconfig.app.json`
- Create: `web/tsconfig.node.json`
- Create: `web/tailwind.config.ts`
- Create: `web/postcss.config.js`
- Create: `web/env.d.ts`
- Create: `web/src/main.ts`
- Create: `web/src/App.vue`
- Create: `web/src/style.css`

- [ ] **Step 1: 用 Bun + Vite 初始化 Vue 3 + TypeScript 项目**

```bash
mkdir web
cd web
bun create vite . --template vue-ts
```

- [ ] **Step 2: 安装依赖**

```bash
cd web
bun add lightweight-charts
bun add -d tailwindcss postcss autoprefixer daisyui@4
```

- [ ] **Step 3: 初始化 Tailwind CSS**

```bash
cd web
bunx tailwindcss init -p
```

- [ ] **Step 4: 配置 tailwind.config.ts**

```ts
import type { Config } from 'tailwindcss'

export default {
  content: [
    "./index.html",
    "./src/**/*.{vue,js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [require("daisyui")],
  daisyui: {
    themes: ["dark"],
  },
} satisfies Config
```

- [ ] **Step 5: 配置 vite.config.ts，设置 build 输出到 ../frontend**

```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
    },
  },
  build: {
    outDir: resolve(__dirname, '../frontend'),
    emptyOutDir: false,
  },
})
```

注意：`emptyOutDir: false` 防止 build 时删除 data.json。

- [ ] **Step 6: 配置 env.d.ts 声明 lightweight-charts 模块**

```ts
/// <reference types="vite/client" />
```

- [ ] **Step 7: 写 src/style.css**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

html, body, #app {
  height: 100%;
  margin: 0;
  overflow: hidden;
}
```

- [ ] **Step 8: 写 src/main.ts**

```ts
import { createApp } from 'vue'
import App from './App.vue'
import './style.css'

createApp(App).mount('#app')
```

- [ ] **Step 9: 写 src/App.vue 骨架**

```vue
<script setup lang="ts">
</script>

<template>
  <div class="h-full flex flex-col" data-theme="dark">
    <div class="navbar bg-base-100 border-b border-base-300 px-4 min-h-[48px]">
      <div class="flex-1 gap-2">
        <span class="text-lg font-bold">SocialDex</span>
        <span class="text-xs opacity-40">社会思潮指数</span>
      </div>
      <div class="flex-none">
        <span class="text-xs opacity-40">加载中...</span>
      </div>
    </div>
    <div class="flex flex-1 overflow-hidden">
      <div class="w-56 border-r border-base-300 bg-base-100 overflow-y-auto">
        <!-- Sidebar placeholder -->
      </div>
      <div class="flex-1 flex flex-col">
        <!-- Chart area placeholder -->
        <div class="flex-1 flex items-center justify-center opacity-30">
          加载中...
        </div>
      </div>
    </div>
  </div>
</template>
```

- [ ] **Step 10: 运行 dev 验证**

```bash
cd web
bun run dev
```

预期：浏览器打开后能看到深色主题的骨架页面（navbar + 左侧边栏 + 右侧占位）。

- [ ] **Step 11: 运行 build 验证**

```bash
cd web
bun run build
```

预期：`frontend/` 目录下生成 `index.html` 和 `assets/`，不影响已有的 `data.json`。

- [ ] **Step 12: Commit**

```bash
git add web/
git commit -m "🎉 feat(web): 初始化 Vue 3 + Vite + DaisyUI 项目"
```

---

## Task 2: 定义 TypeScript 类型

**Files:**
- Create: `web/src/types.ts`

- [ ] **Step 1: 写 types.ts**

```ts
/** data.json 的完整结构 */
export interface DexData {
  meta: Meta
  series: Record<string, TimePoint[]>
  summary: Record<string, Summary>
}

export interface Meta {
  updated_at: string
  tags: string[]
  authors: AuthorInfo[]
}

export interface AuthorInfo {
  id: number
  name: string
  platform: string
  tags: string[]
}

export interface TimePoint {
  time: string
  value: number
}

export interface Summary {
  latest: number
  change_1d: number
  change_1d_pct: number
  change_7d: number
  change_7d_pct: number
}

/** series key 的类型前缀 */
export type SeriesKey =
  | `tag:${string}`
  | `author:${string}`
  | `index:${string}`

/** 行情面板中一个条目 */
export interface StockItem {
  key: string
  name: string
  latest: number
  change_pct: number
  kind: 'tag' | 'author' | 'index'
}

/** 时间范围选项 */
export type TimeRange = '1d' | '1w' | '1m' | '3m' | '6m' | 'all'
```

- [ ] **Step 2: Commit**

```bash
git add web/src/types.ts
git commit -m "📦 feat(web): 定义 TypeScript 类型"
```

---

## Task 3: 数据加载 composable

**Files:**
- Create: `web/src/composables/useData.ts`

- [ ] **Step 1: 写 useData.ts**

```ts
import { ref, computed } from 'vue'
import type { DexData, StockItem, SeriesKey, TimeRange } from '../types'

const data = ref<DexData | null>(null)
const loading = ref(true)
const error = ref<string | null>(null)
const selectedKeys = ref<string[]>([])
const timeRange = ref<TimeRange>('all')

export function useData() {
  async function load() {
    loading.value = true
    error.value = null
    try {
      const resp = await fetch('./data.json')
      if (!resp.ok) throw new Error(`HTTP ${resp.status}`)
      data.value = await resp.json()
      // 默认选中蓝筹100
      if (data.value) {
        selectedKeys.value = ['index:蓝筹100']
      }
    } catch (e: any) {
      error.value = e.message
    } finally {
      loading.value = false
    }
  }

  /** 从 series key 中提取显示名 */
  function getName(key: string): string {
    if (key.startsWith('tag:')) return key.slice(4)
    if (key.startsWith('author:')) return key.slice(7)
    if (key.startsWith('index:')) return key.slice(6)
    return key
  }

  /** 行情面板列表 */
  const stockItems = computed<StockItem[]>(() => {
    if (!data.value) return []
    const items: StockItem[] = []
    for (const [key, summary] of Object.entries(data.value.summary)) {
      const kind = key.startsWith('tag:') ? 'tag'
        : key.startsWith('author:') ? 'author'
        : 'index'
      items.push({
        key,
        name: getName(key),
        latest: summary.latest,
        change_pct: summary.change_1d_pct,
        kind,
      })
    }
    // 按涨跌幅降序
    return items.sort((a, b) => b.change_pct - a.change_pct)
  })

  /** 当前选中的 series 数据 */
  const selectedSeries = computed(() => {
    if (!data.value) return []
    return selectedKeys.value
      .filter(k => k in data.value!.series)
      .map(k => ({
        key: k,
        name: getName(k),
        data: data.value!.series[k],
      }))
  })

  function toggleKey(key: string) {
    const idx = selectedKeys.value.indexOf(key)
    if (idx >= 0) {
      selectedKeys.value.splice(idx, 1)
    } else {
      selectedKeys.value.push(key)
    }
  }

  function selectKey(key: string) {
    selectedKeys.value = [key]
  }

  return {
    data,
    loading,
    error,
    selectedKeys,
    timeRange,
    stockItems,
    selectedSeries,
    load,
    getName,
    toggleKey,
    selectKey,
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add web/src/composables/useData.ts
git commit -m "✨ feat(web): 添加数据加载 composable"
```

---

## Task 4: Sidebar 组件

**Files:**
- Create: `web/src/components/Sidebar.vue`

- [ ] **Step 1: 写 Sidebar.vue**

```vue
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useData } from '../composables/useData'
import type { StockItem } from '../types'

const { stockItems, selectedKeys, selectKey, toggleKey } = useData()

type Filter = 'all' | 'tag' | 'author' | 'index'
const filter = ref<Filter>('all')

const filteredItems = computed(() => {
  if (filter.value === 'all') return stockItems.value
  return stockItems.value.filter(i => i.kind === filter.value)
})

function isSelected(key: string): boolean {
  return selectedKeys.value.includes(key)
}

function handleClick(item: StockItem, e: MouseEvent) {
  if (e.ctrlKey || e.metaKey) {
    toggleKey(item.key)
  } else {
    selectKey(item.key)
  }
}

function formatChange(pct: number): string {
  const sign = pct > 0 ? '+' : ''
  return `${sign}${pct.toFixed(2)}%`
}
</script>

<template>
  <div class="h-full flex flex-col">
    <div class="px-3 py-2 text-xs font-semibold opacity-50 uppercase">自选行情</div>

    <!-- 筛选 tabs -->
    <div class="tabs tabs-bordered px-2 text-xs mb-1">
      <a class="tab" :class="{ 'tab-active': filter === 'all' }" @click="filter = 'all'">全部</a>
      <a class="tab" :class="{ 'tab-active': filter === 'index' }" @click="filter = 'index'">指数</a>
      <a class="tab" :class="{ 'tab-active': filter === 'tag' }" @click="filter = 'tag'">板块</a>
      <a class="tab" :class="{ 'tab-active': filter === 'author' }" @click="filter = 'author'">个股</a>
    </div>

    <!-- 行情列表 -->
    <div class="flex-1 overflow-y-auto">
      <div
        v-for="item in filteredItems"
        :key="item.key"
        class="px-3 py-2 cursor-pointer border-b border-base-200 hover:bg-base-200 transition-colors"
        :class="{ 'bg-base-200': isSelected(item.key) }"
        @click="handleClick(item, $event)"
      >
        <div class="flex items-center justify-between">
          <span class="text-sm font-medium truncate">{{ item.name }}</span>
          <span
            class="text-xs font-mono"
            :class="item.change_pct >= 0 ? 'text-red-400' : 'text-green-400'"
          >
            {{ formatChange(item.change_pct) }}
          </span>
        </div>
        <div class="text-xs opacity-50 font-mono mt-0.5">
          {{ item.latest.toFixed(2) }} 万
        </div>
      </div>
    </div>
  </div>
</template>
```

- [ ] **Step 2: Commit**

```bash
git add web/src/components/Sidebar.vue
git commit -m "✨ feat(web): 添加侧边栏行情面板组件"
```

---

## Task 5: TimeControl 组件

**Files:**
- Create: `web/src/components/TimeControl.vue`

- [ ] **Step 1: 写 TimeControl.vue**

```vue
<script setup lang="ts">
import { useData } from '../composables/useData'
import type { TimeRange } from '../types'

const { timeRange } = useData()

const ranges: { label: string; value: TimeRange }[] = [
  { label: '1天', value: '1d' },
  { label: '1周', value: '1w' },
  { label: '1月', value: '1m' },
  { label: '3月', value: '3m' },
  { label: '6月', value: '6m' },
  { label: '全部', value: 'all' },
]
</script>

<template>
  <div class="flex items-center gap-1 px-4 py-2 border-t border-base-300 bg-base-100">
    <button
      v-for="r in ranges"
      :key="r.value"
      class="btn btn-xs"
      :class="timeRange === r.value ? 'btn-primary' : 'btn-ghost'"
      @click="timeRange = r.value"
    >
      {{ r.label }}
    </button>
  </div>
</template>
```

- [ ] **Step 2: Commit**

```bash
git add web/src/components/TimeControl.vue
git commit -m "✨ feat(web): 添加时间区间控制组件"
```

---

## Task 6: ChartPanel 组件（Lightweight Charts）

**Files:**
- Create: `web/src/components/ChartPanel.vue`

- [ ] **Step 1: 写 ChartPanel.vue**

```vue
<script setup lang="ts">
import { ref, watch, onMounted, onUnmounted, computed } from 'vue'
import { createChart, type IChartApi, type ISeriesApi, ColorType } from 'lightweight-charts'
import { useData } from '../composables/useData'

const chartContainer = ref<HTMLDivElement>()
let chart: IChartApi | null = null
let seriesMap = new Map<string, ISeriesApi<'Area'>>()

const { selectedSeries, timeRange } = useData()

/** 根据 timeRange 过滤数据的时间范围 */
function filterByRange(data: { time: string; value: number }[]) {
  if (timeRange.value === 'all') return data
  const now = new Date()
  let cutoff: Date
  switch (timeRange.value) {
    case '1d': cutoff = new Date(now.getTime() - 86400000); break
    case '1w': cutoff = new Date(now.getTime() - 7 * 86400000); break
    case '1m': cutoff = new Date(now.getTime() - 30 * 86400000); break
    case '3m': cutoff = new Date(now.getTime() - 90 * 86400000); break
    case '6m': cutoff = new Date(now.getTime() - 180 * 86400000); break
    default: return data
  }
  return data.filter(d => new Date(d.time) >= cutoff)
}

/** 为每条曲线分配颜色 */
const COLORS = [
  '#2962ff', '#ff6d00', '#00e676', '#d500f9',
  '#ff1744', '#00b0ff', '#ffd600', '#1de9b6',
]

function initChart() {
  if (!chartContainer.value) return
  chart = createChart(chartContainer.value, {
    layout: {
      background: { type: ColorType.Solid, color: '#1a1a2e' },
      textColor: '#6b7f99',
    },
    grid: {
      vertLines: { color: '#1f2937' },
      horzLines: { color: '#1f2937' },
    },
    crosshair: { mode: 0 },
    rightPriceScale: { borderColor: '#2a3240' },
    timeScale: {
      borderColor: '#2a3240',
      timeVisible: true,
      secondsVisible: false,
    },
    handleScale: { axisPressedMouseMove: true },
    handleScroll: { vertTouchDrag: false },
  })
}

function renderSeries() {
  if (!chart) return

  // 清除旧 series
  for (const [, s] of seriesMap) {
    chart.removeSeries(s)
  }
  seriesMap.clear()

  // 添加新 series
  selectedSeries.value.forEach((s, i) => {
    const filtered = filterByRange(s.data)
    const color = COLORS[i % COLORS.length]
    const areaSeries = chart!.addAreaSeries({
      lineColor: color,
      topColor: color + '33',
      bottomColor: color + '05',
      lineWidth: 2,
      title: s.name,
    })
    areaSeries.setData(filtered.map(d => ({
      time: d.time.replace(' ', 'T'),
      value: d.value,
    })))
    seriesMap.set(s.key, areaSeries)
  })

  chart.timeScale().fitContent()
}

// 监听数据和时间范围变化
watch([selectedSeries, timeRange], () => {
  renderSeries()
}, { deep: true })

onMounted(() => {
  initChart()
  renderSeries()
  const ro = new ResizeObserver(() => chart?.applyOptions({
    width: chartContainer.value?.clientWidth,
    height: chartContainer.value?.clientHeight,
  }))
  ro.observe(chartContainer.value!)
  onUnmounted(() => ro.disconnect())
})

onUnmounted(() => {
  chart?.remove()
  chart = null
})
</script>

<template>
  <div ref="chartContainer" class="flex-1 w-full" />
</template>
```

- [ ] **Step 2: Commit**

```bash
git add web/src/components/ChartPanel.vue
git commit -m "✨ feat(web): 添加 Lightweight Charts 图表组件"
```

---

## Task 7: 组装 App.vue

**Files:**
- Modify: `web/src/App.vue`

- [ ] **Step 1: 重写 App.vue**

```vue
<script setup lang="ts">
import { onMounted } from 'vue'
import { useData } from './composables/useData'
import Sidebar from './components/Sidebar.vue'
import ChartPanel from './components/ChartPanel.vue'
import TimeControl from './components/TimeControl.vue'

const { data, loading, error, load } = useData()

onMounted(() => load())
</script>

<template>
  <div class="h-full flex flex-col" data-theme="dark">
    <!-- Navbar -->
    <div class="navbar bg-base-100 border-b border-base-300 px-4 min-h-[48px]">
      <div class="flex-1 gap-2">
        <span class="text-lg font-bold">SocialDex</span>
        <span class="text-xs opacity-40">社会思潮指数 - 基于自媒体粉丝量的板块轮动观察</span>
      </div>
      <div class="flex-none">
        <span v-if="data" class="text-xs opacity-40">
          更新至 {{ data.meta.updated_at.replace('T', ' ').slice(0, 16) }}
        </span>
      </div>
    </div>

    <!-- 主体 -->
    <div class="flex flex-1 overflow-hidden">
      <!-- 左侧边栏 -->
      <aside class="w-56 border-r border-base-300 bg-base-100 flex-shrink-0">
        <Sidebar />
      </aside>

      <!-- 右侧图表区 -->
      <main class="flex-1 flex flex-col bg-[#1a1a2e]">
        <!-- 加载状态 -->
        <div v-if="loading" class="flex-1 flex items-center justify-center">
          <span class="loading loading-spinner loading-lg text-primary"></span>
        </div>

        <!-- 错误状态 -->
        <div v-else-if="error" class="flex-1 flex items-center justify-center">
          <div class="text-center">
            <p class="text-xl mb-2">数据加载失败</p>
            <p class="text-sm opacity-50">{{ error }}</p>
          </div>
        </div>

        <!-- 图表 -->
        <template v-else>
          <ChartPanel />
          <TimeControl />
        </template>
      </main>
    </div>
  </div>
</template>
```

- [ ] **Step 2: 运行 dev 验证页面结构**

```bash
cd web
bun run dev
```

预期：能看到 navbar + 左侧边栏（空的，因为 data.json 还没更新）+ 右侧加载失败提示。

- [ ] **Step 3: Commit**

```bash
git add web/src/App.vue
git commit -m "✨ feat(web): 组装完整页面布局"
```

---

## Task 8: 改造后端 generate.py

**Files:**
- Modify: `src/generate.py`

- [ ] **Step 1: 重写 generate.py，生成完整的 data.json**

```python
import json
import sqlite3
from pathlib import Path
from database import DB_PATH, CONFIG_PATH

FRONTEND_DIR = Path(__file__).parent.parent / "frontend"


def load_config():
    if not CONFIG_PATH.exists():
        return {"authors": []}
    with open(CONFIG_PATH, 'r', encoding='utf-8') as f:
        return json.load(f)


def generate_data():
    """生成包含 meta/series/summary 的 data.json"""
    FRONTEND_DIR.mkdir(exist_ok=True)
    conn = sqlite3.connect(DB_PATH)
    c = conn.cursor()

    config = load_config()

    # --- meta ---
    c.execute('SELECT DISTINCT tag FROM author_tags ORDER BY tag')
    tags = [r[0] for r in c.fetchall()]

    c.execute('SELECT a.id, a.name, a.platform FROM authors a ORDER BY a.id')
    authors = [
        {"id": r[0], "name": r[1], "platform": r[2], "tags": []}
        for r in c.fetchall()
    ]
    # 填充每个 author 的 tags
    for author in authors:
        c.execute('SELECT tag FROM author_tags WHERE author_id = ?', (author["id"],))
        author["tags"] = [r[0] for r in c.fetchall()]

    # 获取所有时间点
    c.execute('SELECT DISTINCT recorded_at FROM follower_history ORDER BY recorded_at')
    timestamps = [r[0] for r in c.fetchall()]

    updated_at = timestamps[-1] if timestamps else ""

    meta = {
        "updated_at": updated_at,
        "tags": tags,
        "authors": authors,
    }

    # --- series ---
    series = {}

    # 1. 每个 tag 的汇总曲线
    for tag in tags:
        c.execute('SELECT author_id FROM author_tags WHERE tag = ?', (tag,))
        author_ids = [r[0] for r in c.fetchall()]
        series[f"tag:{tag}"] = _build_series(c, author_ids, timestamps)

    # 2. 蓝筹100
    c.execute('''
        SELECT a.id
        FROM authors a
        JOIN follower_history h ON a.id = h.author_id
        WHERE h.recorded_at = (SELECT MAX(recorded_at) FROM follower_history)
        ORDER BY h.followers_count DESC
        LIMIT 100
    ''')
    blue_chip_ids = [r[0] for r in c.fetchall()]
    series["index:蓝筹100"] = _build_series(c, blue_chip_ids, timestamps)

    # 3. 每个 author 的单独曲线
    for author in authors:
        c.execute('''
            SELECT recorded_at, followers_count
            FROM follower_history
            WHERE author_id = ?
            ORDER BY recorded_at
        ''', (author["id"],))
        rows = c.fetchall()
        # 去重：同一时间点取最新
        seen = set()
        pts = []
        for ts, val in reversed(rows):
            if ts not in seen:
                seen.add(ts)
                pts.append({"time": ts, "value": round(val / 10000, 2)})
        pts.reverse()
        series[f"author:{author['name']}"] = pts

    # --- summary ---
    summary = {}
    for key, pts in series.items():
        if not pts:
            continue
        latest_val = pts[-1]["value"]
        latest_time = pts[-1]["time"]

        base_1d = _find_base(pts, latest_time, days=1)
        base_7d = _find_base(pts, latest_time, days=7)

        summary[key] = {
            "latest": latest_val,
            "change_1d": round(latest_val - base_1d, 2),
            "change_1d_pct": _safe_pct(latest_val, base_1d),
            "change_7d": round(latest_val - base_7d, 2),
            "change_7d_pct": _safe_pct(latest_val, base_7d),
        }

    conn.close()

    output = {
        "meta": meta,
        "series": series,
        "summary": summary,
    }

    with open(FRONTEND_DIR / "data.json", "w", encoding="utf-8") as f:
        json.dump(output, f, ensure_ascii=False)

    print(f"数据已生成到 {FRONTEND_DIR / 'data.json'}")


def _build_series(c, author_ids: list[int], timestamps: list[str]) -> list[dict]:
    """为指定 author_ids 构建汇总时间序列"""
    if not author_ids:
        return []
    series = []
    for ts in timestamps:
        c.execute('''
            SELECT SUM(h.followers_count)
            FROM follower_history h
            WHERE h.author_id IN ({})
            AND h.recorded_at = (
                SELECT MAX(recorded_at)
                FROM follower_history h2
                WHERE h2.author_id = h.author_id AND h2.recorded_at <= ?
            )
        '''.format(','.join('?' * len(author_ids))), author_ids + [ts])
        total = c.fetchone()[0] or 0
        series.append({"time": ts, "value": round(total / 10000, 2)})
    return series


def _find_base(pts: list[dict], latest_time: str, days: int) -> float:
    """找到 N 天前最近的数据点的 value"""
    from datetime import datetime, timedelta
    latest_dt = datetime.strptime(latest_time, "%Y-%m-%d %H:%M:%S")
    cutoff = latest_dt - timedelta(days=days)
    cutoff_str = cutoff.strftime("%Y-%m-%d %H:%M:%S")
    # 找 cutoff 之后第一个点
    for p in pts:
        if p["time"] >= cutoff_str:
            return p["value"]
    return pts[0]["value"] if pts else 0


def _safe_pct(current: float, base: float) -> float:
    """安全计算百分比变动"""
    if base == 0:
        return 0.0
    return round((current - base) / base * 100, 2)


if __name__ == "__main__":
    generate_data()
```

- [ ] **Step 2: 运行 generate.py 验证输出**

```bash
cd /c/Users/Dao/Code/dao1023/SocialDex
python -m src.generate
```

预期：`frontend/data.json` 被更新，包含 `meta`、`series`、`summary` 三个顶层 key。可以验证：

```bash
python -c "import json; d=json.load(open('frontend/data.json')); print(list(d.keys())); print('series keys:', list(d['series'].keys())[:5]); print('summary keys:', list(d['summary'].keys())[:5])"
```

- [ ] **Step 3: 更新 run.py 引用**

run.py 已经引用 `generate_data`，不需要修改。验证：

```bash
python -c "from src.generate import generate_data; print('import ok')"
```

- [ ] **Step 4: Commit**

```bash
git add src/generate.py frontend/data.json
git commit -m "✨ feat: 改造 generate.py 输出完整数据（tag+账号+涨跌幅）"
```

---

## Task 9: 前端联调 + 构建

**Files:**
- Modify: `web/src/App.vue`（微调）
- Modify: `frontend/index.html`（build 产物）

- [ ] **Step 1: 启动前端 dev server，打开浏览器确认数据加载正常**

```bash
cd web
bun run dev
```

预期：左侧边栏显示行情列表（蓝筹100、各 tag、各账号），右侧图表显示曲线。点击左侧条目切换图表。

- [ ] **Step 2: 修复联调中发现的问题**

常见问题：
- data.json 的时间格式可能带空格，Lightweight Charts 需要 ISO 格式 → 已在 ChartPanel 中用 `replace(' ', 'T')` 处理
- 涨跌幅排序方向 → 确认 A 股风格红涨绿跌
- 侧边栏宽度不够 → 调整 `w-56`

- [ ] **Step 3: Build 前端**

```bash
cd web
bun run build
```

- [ ] **Step 4: 验证 build 产物**

```bash
ls -la frontend/
```

预期：`frontend/` 下有 `index.html`、`assets/` 目录、`data.json` 仍在。

- [ ] **Step 5: 用静态服务器验证 build 产物**

```bash
cd frontend
python -m http.server 8080
```

打开 `http://localhost:8080` 验证完整功能。

- [ ] **Step 6: Commit**

```bash
git add web/ frontend/
git commit -m "✨ feat: 完成股票风格 UI，前后端联调通过"
```

---

## Task 10: 清理旧文件 + 更新 .gitignore

**Files:**
- Modify: `.gitignore`
- Remove: `frontend/index.html`（旧的，会被 build 覆盖）

- [ ] **Step 1: 更新 .gitignore，忽略 web/node_modules 和 frontend/assets**

```
web/node_modules/
```

注意：`frontend/index.html` 和 `frontend/assets/` 应该被 git track（因为是部署产物），但 `frontend/data.json` 已经在 track 中。

- [ ] **Step 2: 确认 .github/workflows/static.yml 是否需要调整**

检查 GitHub Actions 的部署流程是否兼容新的 build 产物结构。如果之前直接部署 frontend/ 目录，应该无需改动。

- [ ] **Step 3: Commit**

```bash
git add .gitignore
git commit -m "🔧 chore: 更新 .gitignore，清理构建配置"
```
